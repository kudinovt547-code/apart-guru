# Руководство по импорту данных Apart Guru

## 📋 Обзор

Apart Guru поддерживает импорт данных из внешних источников (Excel/JSON) с автоматической нормализацией и валидацией.

## 🗂️ Структура директорий

```
data/
├── inbox/               # Входящие файлы
│   ├── README.txt       # Инструкция
│   ├── EXAMPLE_STRUCTURE.md  # Структура данных
│   └── [ваши файлы]     # Excel/JSON файлы
├── content/             # Обработанные данные
│   └── stats.generated.json  # Автоматически генерируется
└── processed/           # Промежуточные результаты (опционально)
```

## 🚀 Быстрый старт

### Шаг 1: Подготовить данные

Создайте файл в `data/inbox/`:
- **JSON**: `ApartPro_Stats_AllObjects_for_Claude_v1.json` (приоритетнее)
- **Excel**: `ApartPro_Stats_AllObjects_for_Claude_v1.xlsx` (конвертируется в JSON)

### Шаг 2: Запустить импорт

```bash
npm run import:stats
```

### Шаг 3: Проверить результат

Скрипт выведет отчёт:
```
📈 IMPORT REPORT

Total objects: 45
With seasonality data: 42

🏆 Top 10 by ₽/м²/мес:
1. Студия в центре Сочи (Сочи) — 2450 ₽/м²/мес
2. Лофт у Казанского Кремля (Казань) — 1920 ₽/м²/мес
...

✅ Data exported to: /path/to/stats.generated.json
```

### Шаг 4: Запустить сайт

```bash
npm run dev
# или
npm run build && npm start
```

Сайт автоматически использует импортированные данные.

## 📝 Формат данных

### Excel файл

Минимальные обязательные колонки:
- `title` - Название объекта
- `city` - Город
- `country` - Страна
- `price` - Цена в рублях
- `area` - Площадь в м²
- `revPerM2Month` - Доходность ₽/м²/мес
- `noiYear` - NOI в год
- `paybackYears` - Окупаемость (лет)
- `occupancy` - Загрузка (%)
- `adr` - Средний чек (₽)

Опциональные колонки:
- `format` - Формат (apartment/hotel/hostel/villa)
- `status` - Статус (active/planning)
- `riskLevel` - Риск (low/medium/high)
- `summary` - Краткое описание
- `seasonality` - 12 значений через запятую (пример: `60,65,70,80,90,100,100,95,85,75,65,60`)

См. `data/inbox/EXAMPLE_STRUCTURE.md` для деталей.

### JSON файл

```json
{
  "objects": [
    {
      "slug": "sochi-studio",
      "title": "Студия в Сочи",
      "city": "Сочи",
      "country": "Россия",
      "format": "apartment",
      "status": "active",
      "price": 7500000,
      "area": 35,
      "revPerM2Month": 2450,
      "noiYear": 840000,
      "paybackYears": 8.9,
      "occupancy": 78,
      "adr": 3800,
      "riskLevel": "medium",
      "summary": "Студия у моря с высокой загрузкой",
      "why": ["Близко к морю", "Высокий сезон 9 месяцев"],
      "risks": ["Конкуренция", "Зависимость от туристов"],
      "seasonality": [45, 50, 65, 75, 85, 95, 100, 100, 90, 75, 60, 50]
    }
  ],
  "sources": {
    "updatedAt": "2026-01-21",
    "source": "ApartPro Analytics"
  }
}
```

## 🔄 Логика работы

1. **Приоритет JSON**: Если есть `.json` файл, он используется напрямую
2. **Fallback на Excel**: Если только `.xlsx`, он парсится и конвертируется
3. **Нормализация**: Данные проверяются и дополняются значениями по умолчанию
4. **Генерация**: Создаётся `data/content/stats.generated.json`
5. **Использование**: Сайт читает из `stats.generated.json` или fallback на `src/data/projects.ts`

## 🛠️ Скрипты

### `npm run import:stats`
Основной скрипт импорта данных. Запускает `scripts/import-stats.ts`.

**Что делает**:
- Ищет файлы в `data/inbox/`
- Парсит Excel или копирует JSON
- Нормализует структуру данных
- Генерирует `data/content/stats.generated.json`
- Выводит детальный отчёт

### Вспомогательные скрипты

**scripts/create-test-data.ts** (для разработки):
```bash
npx tsx scripts/create-test-data.ts
```
Создаёт тестовый JSON файл из первых 5 проектов `src/data/projects.ts`.

## 🐛 Устранение неполадок

### Ошибка: "No data files found"

**Причина**: Файлы не найдены в `data/inbox/`

**Решение**:
1. Проверьте имя файла (должно точно совпадать)
2. Проверьте расширение (.json или .xlsx)
3. Убедитесь, что файл находится в правильной директории

### Ошибка при парсинге Excel

**Причина**: Неверная структура колонок

**Решение**:
1. Проверьте заголовки колонок (первая строка)
2. Убедитесь, что обязательные колонки присутствуют
3. См. `data/inbox/EXAMPLE_STRUCTURE.md` для примера

### Сайт не использует импортированные данные

**Причина**: Требуется перезапуск dev сервера или rebuild

**Решение**:
```bash
# Остановите dev сервер (Ctrl+C)
npm run dev

# Или пересоберите проект
npm run build
```

### Ошибка "Module not found: stats.generated.json"

**Причина**: Файл не сгенерирован, используется fallback

**Это нормально!** Если файл отсутствует, сайт автоматически использует данные из `src/data/projects.ts`. Это не ошибка, а fallback механизм.

## 📊 Валидация данных

При импорте проверяется:
- Наличие обязательных полей
- Типы данных (number/string)
- Валидность значений (occupancy 0-100%)
- Структура seasonality (ровно 12 значений)

Если данные не валидны, скрипт выведет предупреждения, но попытается импортировать с дефолтными значениями.

## 🔐 Безопасность

- Все импортированные файлы в `.gitignore`
- `stats.generated.json` не коммитится в репозиторий
- Приватные данные остаются локально
- Никакие данные не отправляются на внешние серверы

## 📦 Зависимости

- `xlsx` - Парсинг Excel файлов
- `tsx` - Запуск TypeScript скриптов
- Node.js встроенные модули: `fs`, `path`

## 🎯 Best Practices

1. **Версионирование**: Используйте имена файлов с версиями (`_v1`, `_v2`)
2. **Бэкапы**: Храните резервные копии исходных файлов
3. **Проверка**: Всегда проверяйте отчёт после импорта
4. **Тестирование**: Используйте `scripts/create-test-data.ts` для тестов
5. **Документация**: Обновляйте `EXAMPLE_STRUCTURE.md` при изменении схемы

## 🚀 Расширение системы

### Добавление новых полей

1. Обновите TypeScript типы в `src/types/project.ts`
2. Обновите парсер в `scripts/import-stats.ts`
3. Обновите документацию в `data/inbox/EXAMPLE_STRUCTURE.md`
4. Обновите страницы для отображения новых полей

### Поддержка других форматов

Добавьте парсер в `scripts/import-stats.ts`:
```typescript
function parseCSV(filePath: string): StatsData {
  // Ваша логика парсинга CSV
}
```

## 📞 Поддержка

При проблемах:
1. Проверьте документацию выше
2. Посмотрите примеры в `data/inbox/EXAMPLE_STRUCTURE.md`
3. Проверьте консоль на наличие ошибок
4. Создайте issue в репозитории проекта

---

**Версия**: 1.0
**Обновлено**: 21 января 2026
