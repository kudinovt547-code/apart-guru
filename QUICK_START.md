# 🚀 Quick Start - Apart Guru

## База данных обновлена! (28.01.2026)

✅ **80 объектов** загружено в базу данных
✅ Все метрики рассчитаны автоматически
✅ Калькулятор и статистика готовы к работе

---

## Запустить сайт

```bash
# 1. Запустить dev сервер
npm run dev

# 2. Открыть в браузере
# http://localhost:3000
```

Сайт автоматически использует обновленную базу данных из `data/content/stats.generated.json`.

---

## Что работает

- ✅ **Главная (/)** - таблица всех 80 объектов с сортировкой
- ✅ **/projects** - каталог проектов (карточки)
- ✅ **/projects/[slug]** - детальные страницы объектов
- ✅ **/compare** - сравнение до 5 проектов
- ✅ **/calculator** - калькулятор NOI

---

## Структура базы

```
📁 data/
  ├── content/
  │   └── stats.generated.json    # 80 объектов (главная база)
  ├── inbox/
  │   └── new_data_2026-01-28.json  # Исходные данные

📁 src/
  └── data/
      └── stats.generated.json    # Копия для Next.js
```

---

## Быстрая статистика

```bash
# Количество объектов в базе
cat data/content/stats.generated.json | jq '.objects | length'

# Топ-5 по доходности
cat data/content/stats.generated.json | jq '[.objects[] | select(.status == "active")] | sort_by(-.revPerM2Month) | .[0:5] | .[] | {title, city, revPerM2Month, paybackYears}'

# Города в базе
cat data/content/stats.generated.json | jq '[.objects[].city] | unique'
```

---

## Обновление базы в будущем

```bash
# 1. Положить новые данные в data/inbox/new_data_YYYY-MM-DD.json
# 2. Обновить путь в scripts/convert-new-data.ts (строка 81)
# 3. Запустить конвертацию
npx tsx scripts/convert-new-data.ts

# 4. Перезапустить dev сервер
npm run dev
```

---

## Подробности

См. полный отчет: `DATABASE_UPDATE_2026-01-28.md`

---

**Готово к работе! 🎉**
